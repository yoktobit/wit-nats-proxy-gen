package acme:app@0.0.1;

interface external-function {
    record external-input {
        name: string, 
    }
    handle: func(input: external-input) -> result<string, string>;
}

world provider-world {
    export external-function;
}
